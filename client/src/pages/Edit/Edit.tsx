import * as React from 'react';
import { useSelector } from 'react-redux';
import {
  SearchInput,
} from 'evergreen-ui';

import {
  Page,
} from '@components';


const Edit = () => {
  return (
    <Page>
      Edit
    </Page>
  )
};

export default Edit;
